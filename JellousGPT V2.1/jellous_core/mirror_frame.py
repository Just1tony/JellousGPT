# mirror_frame.py

def reflect(input_text: str) -> str:
    """Simulates a psychological mirror. Reframes the input to reveal hidden emotions or motives."""
    return f"What if the real reason behind this is something you’re afraid to admit?\n→ {input_text}"
